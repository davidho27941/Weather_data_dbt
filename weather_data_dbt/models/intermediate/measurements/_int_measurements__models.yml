version: 2

models:
  - name: int_measurements_aggregate_over_datetime
    description: >
      Per-row rolling aggregates (hour / day / week / month) of weather
      measurements partitioned by station_id. Forms the base for the
      downstream `*_aggregated_measurements` mart models.
    docs:
      show: true
    access: public
    columns: []
